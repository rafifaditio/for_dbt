version: 2

seeds:
  - name: bike_stations
    config:
      column_types:
        station_id: integer
        name: varchar
        status: varchar
        address: varchar
        district: integer
        modified_date: timestamp